import { Link } from "react-router-dom";

function MainPage() {
  return (
    <div>
      mainpage
      <Link to={"calculator"}>Navigate</Link>
    </div>
  );
}

export default MainPage;
